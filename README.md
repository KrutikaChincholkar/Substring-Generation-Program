# Substring-Generation-Program
A program to generate all possible substrings of a given string. A substring is a contiguous sequence of characters within a string. This implementation demonstrates the use of nested loops to extract substrings in C, C++, Java, and Python.
